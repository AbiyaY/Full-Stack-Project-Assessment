import React from "react";
import "./header.css";

const Header = () => {
    return (
        <header className="App-header">
            <h4>Video Recommendation</h4>
        </header>
    );
};

export default Header;